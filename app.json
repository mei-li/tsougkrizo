{
  "environments": {
    "production": {
      "addons": ["heroku-postgresql:hobby-dev"],
      "scripts": {
        "postdeploy": "python create_db.py"
      }
    },
    "review": {
      "addons": ["heroku-postgresql:hobby-dev"],
      "scripts": {
        "postdeploy": "python create_db.py"
      }
    }
  } 
}